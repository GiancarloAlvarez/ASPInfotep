@model IEnumerable<CrudAhorroPrestamos.Models.Rol_usuario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.rol)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuarios.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.rol)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuarios.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_rol_usuario }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_rol_usuario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_rol_usuario })
        </td>
    </tr>
}

</table>
